"""Users domain — anonymous device tracking, sign-up, profile management.

Sub-packages:
- ``routers/`` — FastAPI route declarations (mounted by ``main.create_app``).
- ``services/`` — domain services (transaction-bounded, no HTTP awareness).
"""
